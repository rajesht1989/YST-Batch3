// get 3 no from user identify the smallest number and print on console
#include <stdio.h>

int main() {
    int a=getNumberFromUser();
    int b=getNumberFromUser();
    int c=getNumberFromUser();
    printf("smallest no is %d",(a,b,c));
}
int getNumberFromUser()
{
    printf("Enter the number");
    int value;
    scanf("%d",&value);
    return value;
}
int smallestaNoIs(int a,int b,int c)
{
    if(a<b)
    {
        
        if(a<c)
        {
     printf("smallest no is%d",a);
        
        }
    }
}
    else 
if(b<c)
{
    printf("smallest no is %d",c);
}

