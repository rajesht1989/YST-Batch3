//Requirement:get three numbers from the user and add them and print them on the console

#include <stdio.h>
void main () {
int i=getnumberfromtheuser ();
int j=getnumberfromtheuser ();
int l=getnumberfromtheuser ();
int k=add(i,j,l);
printf("addition of number %d",k);
}
int getnumberfromtheuser () {
printf ("enter a number ");
int v;
scanf ("%d",&v);
return v;
}
int add (int firstno,int secondno)
{
return firstno+secondno;    
}
