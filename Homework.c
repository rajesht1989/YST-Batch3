// get two values from the user and print it and scan it
#include <stdio.h>
int main() {
int firstno;
int secondno;
printf("Enter the firstno ");
scanf("%d",&firstno);
printf("Enter the secondno ");
scanf("%d",&secondno);
int result=firstno/secondno;
printf("Division of the two number is %d", result);
}
    // get two values from the user and print it and scan it
#include <stdio.h>
int main() {
int firstno;
int secondno;
printf("Enter the firstno ");
scanf("%d",&firstno);
printf("Enter the secondno ");
scanf("%d",&secondno);
int result=firstno+secondno;
printf("Addition of the two number is %d", result);
}
// get two values from the user and print it and scan it
#include <stdio.h>

int main() {
int firstno;
int secondno;
printf("Enter the firstno ");
scanf("%d",&firstno);
printf("Enter the secondno ");
scanf("%d",&secondno);
int result=firstno*secondno;
printf("Multiple of the two number is %d", result);
}
    // get two values from the user and print it and scan it
#include <stdio.h>

int main() {
int firstno;
int secondno;
printf("Enter the firstno ");
scanf("%d",&firstno);
printf("Enter the secondno ");
scanf("%d",&secondno);
int result=firstno-secondno;
printf("Sub of the two number is %d", result);
}
    // get three values from the user and print it and scan it
#include <stdio.h>

int main() {
int firstno;
int secondno;
int thirdno;
printf("Enter the firstno ");
scanf("%d",&firstno);
printf("Enter the secondno ");
scanf("%d",&secondno);
printf("Enter the thirdno ");
scanf("%d",&thirdno);
int result=firstno+secondno+thirdno;
printf("Addition of the two number is %d", result);
}
    // get three values from the user and print it and scan it
#include <stdio.h>

int main() {
int firstno;
int secondno;
int thirdno;
printf("Enter the firstno ");
scanf("%d",&firstno);
printf("Enter the secondno ");
scanf("%d",&secondno);
printf("Enter the thirdno ");
scanf("%d",&thirdno);
int result=firstno-secondno-thirdno;
printf("sub of the two number is %d", result);

    
    
