// get no from user identify the smallest number and print on console
#include <stdio.h>
int main() {
int a=getNumberFromUser();
int b=getNumberFromUser();
int c=getNumberFromUser();
int d=getNumberFromUser();
printf("smallest no is %d",(a,b,c,d));
}
int getNumberFromUser()
{
    printf("Enter the number");
    int value;
    scanf("%d",&value);
    return value;
}
int smallest (int a,int b,int c,int d)
{
    if(a<b)
    {
        if(a<c)
        {
            if(a<d)
            {
                printf("smallest no is%d",a);
            }
                    else
{
    printf("smallest no is%d",d);
    
}
}
}
else
if(b<c)
{
    if(b<d)
    {
        printf("smallest no is %d",b);
    }
    else
{
    printf("smallest no is %d",d);
}

                }
                else
if(c<d)
{
    printf("smallest no is%d",c);
}
else
{
    printf("smallest no is%d",d);
}
}
